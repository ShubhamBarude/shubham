package pra2_program;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Stream;

public class Java8StreamProg {
    public static void main(String[] args) {
        List<Object> obj=new ArrayList<>();
        obj.add("shubham");
        obj.add(10);
        obj.add('s');
        obj.add("akash");
      //  obj.stream().limit(2).forEach(System.out::println);
       // obj.stream().skip(3).forEach(System.out::println);
//        Stream<Java8StreamProg> ref=Stream.empty() ;
//        System.out.println(obj);
//        System.out.println(ref.count());
      //  obj.stream().map()
    }

    }

